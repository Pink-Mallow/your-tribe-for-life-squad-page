  {% for person in persons %}
    <div class="card">
      <img class="season-flower"> {%comment%} put seasonal flower in a component {%endcomment%}
      <a href="#{{ person.name | slugify }}" 
      class="slugify">
        {{ person.name }}
      </a>
    </div>
  {% endfor %}

<style>
  .card{
    width: 126px;
    height: 169px;
    background-color: #F7D5BA;
    border-radius: 2px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }
  label{
    text-align: center;
    text-size: 20px;
  }
  img{
    margin-top: 5px;
    width: 110px;
    height: 124px;
    background-color: red;
    border-width: 2px;
  }
</style>

    